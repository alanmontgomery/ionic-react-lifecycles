import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-lifecycle',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
